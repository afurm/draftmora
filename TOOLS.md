# Local Tool Conventions

- Tool paths must stay inside the selected workspace.
- Relative paths resolve from the selected workspace.
- Use commands only when their working directory is inside this workspace.
