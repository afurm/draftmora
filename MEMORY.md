Draftmora uses a built-in memory file pattern: USER.md stores durable user preferences, MEMORY.md stores durable agent and project notes, entries are compact plain text separated by the section-sign delimiter, and explicit remember requests write immediately.
§
For this project, run npm run typecheck, npm test, and npm run build before publishing changes.
